import { LightButton } from '@/components/button/light-button'
import { PlainButton } from '@/components/button/plain-button'
import { PrimaryButton } from '@/components/button/primay-button'

export const Button = {
  Primary: PrimaryButton,
  Plain: PlainButton,
  Light: LightButton,
}
